# Build output - regenerated by `dotnet build`/`dotnet publish`, never hand-edited
bin/
obj/

# Velopack release packaging output - regenerated by `dotnet publish` + `vpk pack`,
# see HANDOFF.md's release steps. Never hand-edited, never committed.
publish/
Releases/

# IDE/editor folders
.vs/
.vscode/
*.user

# OS noise
Thumbs.db
desktop.ini

# Claude Code local settings (permission allowlist etc.) - personal to this machine,
# not project content
.claude/settings.local.json

# WPF markup-compile pass-2 scratch project (see F1RaceEngineer.csproj comment)
*_wpftmp.csproj
